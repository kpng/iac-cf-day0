image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest

stages:
    - build


build:
    stage: build
    before_script:
        - uname -a
        - sudo apt update
    script:
        - make deploy
    artifacts:
        paths:
            - build/
