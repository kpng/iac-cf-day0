image: registry.gitlab.com/gitlab-org/cloud-deploy/aws-base:latest

stages:
    - build


build:
    stage: build
    before_script:
        - uname -a
        - pwd
        - ls /usr/bin/make
        - /usr/bin/make --version
    script:
        - make deploy
    artifacts:
        paths:
            - build/
